# HANDOFF — if another operator picks this up

1. Read in order: `README.md` (orient) → `FEASIBILITY_AUDIT.md` (strategy truth) → `V1_COMPLETION_AUDIT.md` (V1 inventory + priority list + what-NOT-to-build) → `CAPABILITY_REGISTRY.yml` (capability states + change_log) → `VERIFICATION_LOG.md` (every VR) → `GPT_AUDIT.md` (audit ledger + infrastructure absence proof) → `BLOCKERS.md` (open gates) → **`STATE.md` (current HEAD — this file is not the snapshot)**.
2. Never claim beyond evidence; B-1 (external audit gate) is OPEN for everything shipped. Never claim `VERIFIED_PRODUCTION`. Never describe `SENT`/`SUBMITTED` as customer `DELIVERED`.
3. Run: `npm install && npm test` (node_modules is NOT durable across sandbox turns — reinstall each session). Current recorded suite on this `main`: **332/332**. Re-run after any code change.
4. Live drill: `source scripts/drill-env.sh && npm start`, then scripts under `scripts/*drill*` (they sign webhooks correctly with awk `$2` — hard-won lesson).
5. Write access discipline: `.gitignore` keeps runtime `./data` out of git by design (sessions/PII/kill state). `src/data/products.json` (owner-maintained catalog) IS tracked — it was accidentally ignored until V1-0 (2026-09-09); the LLM system prompt embeds its `policies` JSON, so policy strings there are customer-quotable — keep them truthful. Catalog is currently **STALE** until the owner re-verifies.
6. Git identity does NOT persist across sandbox turns — before committing: `git config user.name "$(git log -1 --format='%an')"` + same for email (identity: `sentinel-eng <sentinel@local>`).
7. Current `main` at last docs pass was `9e0f61a…`; this branch updates owner invoices/floors. Historical freeze tag `sentinel-v1.6-pilot-freeze` → `f92a3f8d…` is **not** current HEAD. Reno 16 invoice **199999** / floor **186800**. Reno 16F invoice **149999** / floor **138600** (B-7 closed). Floors are last-resort — never the opening quote, never below, never labelled dealer-price to the customer. B-2 is **BLOCKED / NOT RUN**. Typing indicator is code-verified, not live-proven. Catalog `observed_at` is still **STALE** vs wall clock until the owner re-verifies. **D-010:** CURRENT TRANSPORT = Meta Cloud API; QR/SESSION = NOT IMPLEMENTED.
