1. Read in order: **`SENTINEL_CONTINUATION_STATE.md`** (current checkpoint) → `docs/evidence/M2_FREEZE.md` → `README.md` → `STATE.md` → `CAPABILITY_REGISTRY.yml` → `VERIFICATION_LOG.md`. Then inspect `git status` / `git log` and run the existing suite **before** modifying code.
2. Never claim beyond evidence; B-1 (external audit gate) is OPEN. Never claim `VERIFIED_PRODUCTION`. Never describe `SENT`/`SUBMITTED` as customer `DELIVERED`. M2 is **not** live WhatsApp proof.
3. Run: `npm install && npm test`. Recorded suite at M2 freeze `399afde`: **432/432 ×2**. Re-run after any code change.
4. Live drill: `source scripts/drill-env.sh && npm start`, then scripts under `scripts/*drill*` (they sign webhooks correctly with awk `$2` — hard-won lesson).
5. Write access discipline: `.gitignore` keeps runtime `./data` out of git by design (sessions/PII/kill state). `src/data/products.json` (owner-maintained catalog) IS tracked. Catalog is currently **STALE** until the owner re-verifies.
6. Git identity does NOT persist across sandbox turns — before committing: `git config user.name "$(git log -1 --format='%an')"` + same for email (identity: `sentinel-eng <sentinel@local>`).
7. **M2 = COMPLETE/FROZEN** at `399afde` on `feature/m2-live-blocker-fix` (not on `main` at freeze). **V1-5.1 = COMPLETE/FROZEN.** Overall = **DEMO/PILOT**. DEMO is default transport; Cloud still works; session requires explicit selection. Reno 16 floor **186800**. Current owner file Reno 16F floor **138600 RESOLVED** (V1-5.1 had 16F UNRESOLVED — do not revert without owner instruction). B-2 is **BLOCKED / NOT RUN**. Do not reopen M2 architecture, P2, kill, CAP-008, outbox, or floors. Next intended phase (not started): RAPID PRODUCT CAPABILITY INTEGRATION.