1. Read in order: **`SENTINEL_CONTINUATION_STATE.md`** → `docs/evidence/LIVE_CORE_BASELINE.md` → `docs/evidence/M2_FREEZE.md` → `README.md` → `STATE.md` → `CAPABILITY_REGISTRY.yml` → `VERIFICATION_LOG.md`. Then inspect `git status` / `git log` and run the existing suite **before** modifying code.
2. Never claim beyond evidence; B-1 (external audit gate) is OPEN. Never claim `VERIFIED_PRODUCTION`. Never describe `SENT`/`SUBMITTED` as customer `DELIVERED`. Never label CONNECTED as `CAUGHT_UP`. Dedicated TEST-number 1:1 is **LIVE-PROVEN**; store number is unused; catch-up is **NOT-PROVEN**.
3. Run: `npm install && npm test`. Recorded suite at live-core freeze: **435/435 ×2**. Re-run after any code change.
4. Live drill: `source scripts/drill-env.sh && npm start`, then scripts under `scripts/*drill*` (they sign webhooks correctly with awk `$2` — hard-won lesson).
5. Write access discipline: `.gitignore` keeps runtime `./data` out of git by design (sessions/PII/kill state). `src/data/products.json` (owner-maintained catalog) IS tracked. Catalog is currently **STALE** until the owner re-verifies.
6. Git identity does NOT persist across sandbox turns — before committing: `git config user.name "$(git log -1 --format='%an')"` + same for email (identity: `sentinel-eng <sentinel@local>`).
7. **LIVE CORE CHECKPOINT** at live-tested `8e7cb2be…` plus this preservation commit. **M2 = COMPLETE.** **V1-5.1 = COMPLETE/FROZEN.** Overall = **DEMO/PILOT**. DEMO is default transport; Cloud still works; session requires explicit selection. Reno 16 floor **186800**. Reno 16F floor **138600 RESOLVED**. B-2 is **BLOCKED / NOT RUN**. Do not reopen M2 architecture, P2, kill, CAP-008, outbox, or floors.
