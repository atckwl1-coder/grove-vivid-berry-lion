# HANDOFF — if another operator picks this up

1. Read in order: `README.md` (orient) → `FEASIBILITY_AUDIT.md` (strategy truth) → `V1_COMPLETION_AUDIT.md` (V1 inventory + priority list + what-NOT-to-build) → `CAPABILITY_REGISTRY.yml` (capability states + change_log) → `VERIFICATION_LOG.md` (every VR) → `GPT_AUDIT.md` (audit ledger + infrastructure absence proof) → `BLOCKERS.md` (open gates).
2. Never claim beyond evidence; B-1 (external audit gate) is OPEN for everything shipped.
3. Run: `npm install && npm test` (node_modules is NOT durable across sandbox turns — reinstall each session).
4. Live drill: `source scripts/drill-env.sh && npm start`, then scripts under `scripts/*drill*` (they sign webhooks correctly with awk `$2` — hard-won lesson).
5. Write access discipline: `.gitignore` keeps runtime `./data` out of git by design (sessions/PII/kill state). `src/data/products.json` (owner-maintained catalog) IS tracked — it was accidentally ignored until V1-0 (2026-09-09); the LLM system prompt embeds its `policies` JSON, so policy strings there are customer-quotable — keep them truthful.
6. Git identity does NOT persist across sandbox turns — before committing: `git config user.name "$(git log -1 --format='%an')"` + same for email (identity: `sentinel-eng <sentinel@local>`).
7. Next cycle (needs explicit authorization): **V1-5 REAL-PILOT READINESS** — Meta webhook live config + live-mode smoke list, DEBT-19 cookie/rate-limit hardening, quality-rating unwiring (registry CAP-011 PILOT notes; BLOCKERS B-2). Without it nothing is more than a demo. Full V1 list + dependencies + do-not-build list: `V1_COMPLETION_AUDIT.md`. V1-4 operator notes: the Day-10 satisfaction follow-up is deterministic (dueAt = saleAt+10d in the customers DB `followups` array; 15-min sweep in `src/workers/scheduler.js`); it fires only from a real engine-recorded sale (`outcome=sale` + `verification=customer_statement`); explicit opt-out (existing CAP-002) and CAP-055 both block it; positive reply closes it, issue reply routes to the existing CAP-008 support path (PRODUCT_EXCEPTION) — staff own the diagnosis; no reminder is ever sent (one-shot by design). V1-3 operator notes: negotiation authority is owner-filed in `src/data/negotiation-rules.json` — the bot/learning can NEVER write it; to change a floor or resolve the open **reno16f floor (139,000–142,000 range, currently UNRESOLVED = no autonomous concession)** the owner edits that file (set `floor` + `floor_status: "RESOLVED"`); to tune the concession step use `policy.concession_step_pct` (default 1.0 = 1% of start price per turn). Catalog freshness for reno16/16F follows the V1-2 ritual (`node scripts/verify-catalog.mjs --verify`). V1-1 memory notes: `recentConversation()` is derived-only (no new storage); bound = 12 × 500 chars; interactive replies are NOT in model context. Human-paced safety layer notes (2026-09-11, VR-2026-09-11-01): brain-path replies are paced internally (complete reply first, ONE complete message out) with stale/takeover/DND/kill/session-lost cancellation; ALL knobs live in ONE place — `src/services/humanPaced/config.js` (env-overridable `HP_*`: per-character model, word/punctuation/line adjustments, min/max composition duration, typing flag, breaker thresholds); production defaults are deliberately conservative (calibrated to the existing response-time contract — raising them for production UX needs a timing-contract review against the suite); the negotiation engine (V1-3) and follow-up (V1-4) send paths are deliberately OUTSIDE the layer (un-paced, breaker not counting them); breaker HUMAN_REVIEW surfaces staff via the existing CAP-008 POLICY_LIMIT escalation and clears on human return (no permanent customer blocking); session 401/403 → fail-closed until operator restore (`markSessionAvailable`), no reconnect logic exists by design; this is NOT an anti-ban/detection-bypass capability — never describe it as one.
