# STATE — truthful snapshot (2026-09-11, post human-paced response & conversation safety layer)

- **HEAD:** `8c4b52e` (merge of `feature/human-paced-safety-layer` / `7913ba6`) — human-paced safety layer cycle (prior: `2ad4f99` V1-4 records, `51fb389` V1-4 merge, `f113e50` V1-4 impl)
- **Suite:** `npm test` → deterministic sequential per-file runner (`scripts/run-tests.mjs`); **181/181** (11 files: cap008 19, cap055 14, humanpaced 26, p2firewall 16, phase2a 10, remediation 8, v11memory 12, v12authority 16, v13negotiation 32, v14followup 17, v1truth 11); 5 clean runs this cycle (pre-merge + ×2 post-merge + ×2 stability)
- **Live server (DEMO):** webhook verified-signed; inbox `/inbox`; kill banner; drill creds in `scripts/drill-env.sh` (demo only)
- **Kill switch:** state file `data/killswitch.json`; currently ACTIVE (drills end resumed); AUTOMATION only demo provider
- **Capabilities:** CAP-001/011 PILOT · CAP-003 PILOT (dated, never-fabricated price truth) · **CAP-039 PILOT** (V1-3: bounded deterministic negotiation) · **CAP-032 first touch live via V1-4** (Day-10 satisfaction follow-up; full journey still PROTOTYPE_DESIGN) · CAP-008 PILOT · CAP-055 PILOT · P2-firewall PILOT (+audit-gate BLOCKED for all — B-1 open) · CAP-006 NOT_PROVEN (honest deferral live since V1-0; capability absent)
- **Negotiation authority (V1-3):** the deterministic engine (`src/services/negotiation.js`) owns EVERY number; the LLM only phrases (rule 9: no numbers beyond catalog lines, no discount promises, no approval claims, handoff). Floors live in `src/data/negotiation-rules.json` (owner file, read-only to the bot): **reno16 floor 186,800 RESOLVED** (owner-supplied dealer-price boundary — used exactly, corrected 2026-09-10 per VR-2026-09-10-02; start 200,000 = catalog price) · **reno16f UNRESOLVED** (owner range 139,000–142,000 — engine does NOT guess; no autonomous concession until the owner resolves = open item B-7). Behavior: value-first is a structural gate; concessions = 1% of start per turn (owner-tunable `concession_step_pct`), offer = max(floor, offer−step), floor = final autonomous position, never below floor; ready-to-buy closes at the current highest-approved offer; below-floor → bounded counter → CAP-008 PRICE_EXCEPTION escalation; re-open after walk at the last authorized position. Negotiation starts only from a VERIFIED catalog price (V1-2).
- **Sales skills + learning (V1-3):** `src/data/sales-skills.json` — 12 metadata-structured skills; selection = per-objection priority + learned win-rate (uses ≥ 3); authority_gated skills (SK-09/SK-12) structurally excluded from ranking. Outcomes captured from engine events into the existing customers DB (`db.negotiations`, verification=`customer_statement` — no payment system; a sale = stated acceptance, never a verified payment). Learning re-ranks VALUE tactics ONLY: floors, step policy, permissions, terms, benefits, gated skills are unreachable (no write calls in the engine — static-proven).
- **Catalog (V1-2 + V1-3):** every quoted price carries a deterministic state — VERIFIED quotes "aaj ki price" + evidence{observed_at,VERIFIED} on the existing P2 EVIDENCE stage; STALE = dated "aakhri verified price" + "confirm karein", no stock numbers; UNKNOWN = no number; corrupt → "rates par kaam jaari hai" + handoff. Owner ritual `node scripts/verify-catalog.mjs --verify` (>25% jump refused without `--approve-jump`). New owner-supplied rows: reno16 (200,000, approved value stack — only `claim` strings quotable; Google account-dependent claims structurally unquotable) + reno16f (150,000, no benefits invented, no stock claimed).
- **AI brain (V1-1 + V1-3):** `think()` sends `[system, ...history, user(current)]` — bounded per-customer context (12 × ≤500 chars, derived from the existing DB); rules 7 (history = data), 8 (catalog labels deterministic), 9 (negotiation: engine owns numbers; LLM phrases + hands off).
- **Customer-facing truth status (V1-0 + V1-2 + V1-3):** zero phantom reservation/booking/token claims; zero undated prices; zero fabricated scarcity/urgency/approval/competitor prices/benefits in engine output; no unsupported Google duration/storage claim.
- **Repo notes:** `src/data/products.json` + `src/data/negotiation-rules.json` tracked (owner files); git identity does NOT persist across sandbox turns (see HANDOFF §6).
- **Post-purchase follow-up (V1-4):** ONE owner-approved Day-10 health check per qualifying purchase (`src/services/followups.js`) — qualifying sale = only the state this deployment truthfully establishes (engine-recorded `outcome=sale` + `verification=customer_statement` — never a verified payment, never fabricated; malformed records fail closed). dueAt = saleAt+10d (file-persistent in the existing customers DB `followups` array; 15-min sweep in `src/workers/scheduler.js` bounds "approximately"). Explicit optedOut respected (existing CAP-002; no new consent architecture) · CAP-055 short-circuit while STOPPED + standard two walls per send · every send = the ONE path (P2 firewall → durable outbox, source AI, actionId = follow-up id; send AWAITED — a DENY can never be mis-marked SENT) · human-owned conversation → AUTHZ DENY → SKIPPED_HUMAN. Responses stay a normal conversation: deterministic fast path — positive (haan theek hai / bilkul theek / sab ok) → natural ack + CLOSED; issue (battery/heat/camera/hang/…) → existing CAP-008 escalation (PRODUCT_EXCEPTION) + honest team ack + ESCALATED (no diagnosis, no warranty/replacement promises); non-canonical replies fall through to the existing brain path with deterministic lifecycle bookkeeping. NO chasing (one follow-up, no reminders — terminal status lifecycle) · NO marketing (scanned: zero promotional vocabulary) · LLM not on the decision path (purchase truth / authorization / timing / opt-out / kill / send-permission / warranty all deterministic). Restart/rerun/duplicate-safe via the EXISTING claimEvent() (real-process probe: SENT→0, SCHEDULED-due→fresh 1 / old 0).
- **Human-paced response & conversation safety layer (2026-09-11):** brain-path reply pacing + conversation safety on the existing transport — the COMPLETE reply is generated/validated first (LLM + validation unchanged), then composed internally with bounded per-character pacing (base + bounded variance + word/comma/sentence/line adjustments; total capped min/max; deterministic model, injectable clock/rng) in `src/services/humanPaced/composer.js` (dedicated, independently testable, async-cancellable, single-flight per conversation — a new customer message SUPERSEDES the in-flight composition). Conversation VERSION (read-only over existing state: conv state/updatedAt/history/unread + customer.lastSeen + optedOut) is re-validated at every checkpoint and pre-dispatch — stale / human-takeover / DND / kill-switch / session-lost compositions are CANCELLED, never sent; the recipient receives ONE complete message (per-character sending does not exist). REUSE, no duplication: CAP-008 takeover (`isSuppressed` guard + POLICY_LIMIT escalation for the breaker), CAP-055 kill (STOP wins at the dispatch boundary), P2 firewall + durable outbox (the ONE send path — the composer never calls a provider), existing claimEvent idempotency (no exactly-once claim). Circuit breaker (`breaker.js`): configurable objective counters (turns per window / duplicate inbound+reply cycles / consecutive failed dispatch) — HUMAN_REVIEW surfaces staff via the existing CAP-008 path and clears on human return or window expiry (NO permanent customer blocking; repeated negotiation is NOT spam; the V1-3 engine path is outside the breaker by design). Session health (`sessionHealth.js`): Meta 401/403 = fatal → fail-closed at the dispatch boundary until operator restore; transient errors stay on the existing bounded outbox retry; NO reconnect/QR/session-deletion logic (stateless per-send adapter; no timers in the layer). ONE config source (`config.js`, HP_* env-overridable) — production defaults calibrated to the existing response-time contract (148-char fallback reply ≈77ms p99 so CAP-055 KS4's 152ms/280ms windows hold); the focused suite uses its own explicit HP_* config. 1-to-1 only (no group/broadcast/campaign surface — structurally verified); existing audit convention with PII redaction (new lifecycle events: composition_started/completed/cancelled/superseded, typing_started/stopped, stale_response_discarded, human_takeover_during_composition, kill_switch_cancelled_composition, session_unavailable_during_composition, autonomous_circuit_breaker_pressure/triggered, final_send_dispatched). Typing presence = UX only, no-op in production (the Meta Cloud API adapter has no reliable typing-presence API; the interface is ready). NOT an anti-ban/detection-bypass capability. V1-5 remains NOT started (explicit directive).
- **Do-not-touch without authorization:** CAP-008/055/P2 foundations, CAP-007 marketing, CAP-009, pricing, real WhatsApp, V1-2..V1-5 (each needs its own mandate)
- **Danger surface (sensitive zone):** `data/` (customer DB + messages + sessions + killswitch); redacted zone = logs/audit/demo — V1-1 reads the sensitive zone (own-customer text) but writes nothing new to it
- **Open debts:** DEBT-07 price validator (brain row REDESIGN_REQUIRED), DEBT-09 governor unwired, DEBT-17/18/19/20 as registered; V1-2 addresses catalog authority (staleness) — not the price validator itself
