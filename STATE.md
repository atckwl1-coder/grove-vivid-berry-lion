# STATE — truthful snapshot (2026-09-09, post V1-2 catalog authority semantics)

- **HEAD:** `b62ed87` (merge of `feature/v12-catalog-authority` / `e95fdb9`) — V1-2 catalog-authority cycle
- **Suite:** `npm test` → deterministic sequential per-file runner (`scripts/run-tests.mjs`); **106/106** (8 files: cap008 19, cap055 14, p2firewall 16, phase2a 10, remediation 8, v11memory 12, v12authority 16, v1truth 11); 5 clean runs this cycle (3 pre-merge + 2 post-merge)
- **Live server (DEMO):** webhook verified-signed; inbox `/inbox`; kill banner; drill creds in `scripts/drill-env.sh` (demo only)
- **Kill switch:** state file `data/killswitch.json`; currently ACTIVE (drills end resumed); AUTOMATION only demo provider
- **Capabilities:** CAP-001/011 PILOT · CAP-003 PILOT (V1-2: freshness-aware, dated, never-fabricated price truth; live in demo) · CAP-008 PILOT · CAP-055 PILOT · P2-firewall PILOT (+audit-gate BLOCKED for all — B-1 open) · CAP-006 NOT_PROVEN (honest deferral live since V1-0; capability absent)
- **Catalog authority (V1-2):** every quoted price carries a deterministic state — **VERIFIED** (owner `observed_at` ≤ 24h TTL) quotes as "aaj ki price" + stock + `evidence{observed_at,status:VERIFIED}` (existing P2 EVIDENCE stage, meta on outbox job); **STALE** (>24h) quotes only as dated "aakhri verified price" + "rates kal ke ho sakte hain — confirm karein", no stock numbers; **UNKNOWN** (missing/malformed/future `observed_at`, bad price) quotes NO number; **corrupt file** → "rates par kaam jaari hai" + handoff (no cache, never last-good). Shipped `products.json` is honestly STALE (2026-09-05) until the owner runs `node scripts/verify-catalog.mjs --verify` (>25% price jump refused without `--approve-jump`). The file's only writer is that script — no remote/customer/LLM write path exists (static-proven). LLM prompt carries the labels (rule 8: model phrases, never relabels; owner file always beats LLM memory). EMI table only from VERIFIED prices.
- **AI brain (V1-1):** `think()` sends `[system, ...history, user(current)]` — bounded per-customer context: last 12 eligible entries × ≤500 chars (≤6000 chars), derived from the existing customers-DB (no new storage); system prompt always first + rule 7 (history = data) + rule 8 (catalog labels are deterministic); current turn always last. Demo fallback (no LLM key) unchanged — no LLM, no memory needed.
- **Customer-facing truth status (V1-0 + V1-2):** zero phantom reservation/booking/token/slot claims; zero undated prices in any output (contract acceptance criterion, test-enforced).
- **Repo notes:** `src/data/products.json` tracked (V1-0) + carries `observed_at`/`verified_price` (V1-2); git identity does NOT persist across sandbox turns (see HANDOFF §6).
- **Do-not-touch without authorization:** CAP-008/055/P2 foundations, CAP-007 marketing, CAP-009, pricing, real WhatsApp, V1-2..V1-5 (each needs its own mandate)
- **Danger surface (sensitive zone):** `data/` (customer DB + messages + sessions + killswitch); redacted zone = logs/audit/demo — V1-1 reads the sensitive zone (own-customer text) but writes nothing new to it
- **Open debts:** DEBT-07 price validator (brain row REDESIGN_REQUIRED), DEBT-09 governor unwired, DEBT-17/18/19/20 as registered; V1-2 addresses catalog authority (staleness) — not the price validator itself
